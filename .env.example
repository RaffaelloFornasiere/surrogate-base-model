# Copy to .env and fill in. Loaded via python-dotenv from the repo root.
HF_TOKEN=
# QER judge (gemini-3-flash-preview). auto-mo defaults to OpenRouter; we point
# its client at Google AI Studio instead — see common.make_judge_client.
GOOGLE_AI_STUDIO_API_KEY=
