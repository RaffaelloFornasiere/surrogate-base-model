# exp/04 — cross-organism quirk probe (reference-free)

## Hypothesis

H1. A linear probe on residual activations can tell "this model likes
Italian food" from other OLMo-2-1B fine-tunes, and generalises to organisms
it never saw.
H2. Applied to the iter1 surrogates, the probe score sits closer to the
clean base than to the MO (behaviour removed), but not at the base
(residual representation, cf. exp/03 military FD 0.18–0.33).

Pass criterion for H1: leave-one-organism-out accuracy ≥ 0.9 on held-out
organisms *and* ≥ 0.8 under leave-one-recipe-out (otherwise it is a recipe
detector). If H1 fails the probe is out of the competition.

## Step 0 — activations (shared with 05)

Extract per-token residual activations at layers 7 and 14 for every model
on the neutral set; local scratch on the pod (`outputs/acts/`, ~8 GB fp16,
not pushed), reused by exp/05.

- Models: 12 MOs, 12 surrogates, clean SFT base and clean DPO (the exp/02
  anchors), 4 cross-family surrogates, cake_bake organisms (+2 seed
  replicates, mobfr registry) as probe negatives.
- Prompts: tulu-3-sft chat, n=128, seeded, the same sample for every
  model. Chat template applied. Store the prompt positions plus a greedy
  64-token continuation generated by the MO and fed to every model, so
  positions align across models.
- Diagnostic table (README, not an experiment): per (organism, layer) the
  norms of mean(h_MO − h_SBM) and mean(h_MO − h_base),
  their cosine, and the projection ⟨d_sbm, d_base⟩ / ‖d_base‖². This is
  the "how big is the diff actually" number exp/03 never measured.

## Design

- **Positives**: the 7 italian_food organisms (all checkpoints available in
  the registry, to add rows; all checkpoints of one organism stay in the
  same fold). Mirror run: the 5 military organisms as positives.
- **Negatives**: military_submarine organisms, cake_bake organisms, the
  clean SFT base and clean DPO. This covers every recipe used by the
  positives, so recipe is not a shortcut.
- **Inputs**: step-0 activations, layers 7 and 14; one row per
  (model, prompt) = mean over response positions. Per-token
  rows (with the model label) only as a second pass if mean pooling fails
  H1.
- **Probe**: logistic regression, standardised features, L2 chosen inside
  the training fold. Report accuracy and AUROC per fold with Wilson CIs.
- **CV** (Raffaello, 2026-09-07): hold out a whole recipe group at a time —
  all DPO (integrated + post-hoc mixed/unmixed, 3 models), all FD (2), all
  SDF (2) — on both the positive and the negative side, so the held-out
  recipe is entirely unseen; plus leave-one-organism-out as the finer
  view. Surrogates never enter training.
- **Readout**: probe logit for each surrogate, normalised between its MO
  (1) and the clean base (0) → "residual quirk".
  Also the cross-family surrogates and MOs, which should read as base.

## Cost

Step 0: under an hour on one 4090, no API. Probe: seconds of CPU.

## Files

`extract.py` (step 0 → `outputs/acts/`), `diff_norms.py` (diagnostic
table), `build_rows.py`, `probe.py` (CV + surrogate readout); outputs →
`surrogate-base-model/results` `phase1/iter2/04_linear_probe/`.
